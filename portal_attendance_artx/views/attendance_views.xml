<odoo>
    <data>
        <template id="portal_my_home_sale" name="Show Quotations / Sales Orders" customize_show="True"
                  inherit_id="portal.portal_my_home" priority="20">
            <div class="oe_structure" position="inside">
                <div class="container">
                    <!-- <button id="attendanceBtn" class="btn btn-warning attendanceBtn">
                        <i class="fa fa-sign-in" aria-hidden="true"></i>
                        <span id="btnText">Click to Check In</span>
                    </button> -->

                                
                    <button id="attendanceBtn" class="btn btn-success w-100 attendanceBtn">
                            <i class="fa fa-sign-in fa-3x"></i>
                            <span id="btnText">تسجيل الحضور</span>
                            </button>
                            <small class="d-block text-muted mt-2">
                                أقر أنا الموظف بتسجيل حضوري وانصرافي من مكتبي المخصص، وأن تسجيلي من خارج المكتب أو تسجيلي نيابة عن الغير يعد مخالفة وفق النظام الجزائي لجرائم التزوير حيث نصت المادة السادسة عشرة: "من زوّر في محرر معدّ لإثبات حضور الموظف إلى عمله أو انصرافه منه؛ يعاقب بالسجن مدة لا تتجاوز ثلاثة أشهر وبغرامة لا تزيد على ثلاثين ألف ريال، أو بإحدى هاتين العقوبتين."
                            </small>


                </div>
            </div>
        </template>
        <record id="action_hr_employee_create_portal_user" model="ir.actions.server">
            <field name="name">Create Portal User</field>
            <field name="model_id" ref="hr.model_hr_employee"/>
            <field name="binding_model_id" ref="hr.model_hr_employee"/>
            <field name="binding_view_types">form</field>
            <field name="groups_id" eval="[(4, ref('base.group_erp_manager'))]"/>
            <field name="state">code</field>
            <field name="code">
                action = records.action_create_portal_user()
            </field>
        </record>
    </data>
</odoo>